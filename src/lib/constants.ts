// Re-export constants used across views
import type { ContentTypeColors } from '../types/dashboard';

export const CONTENT_TYPE_COLORS: ContentTypeColors = {
    social: 'primary',
    email: 'info',
    ads: 'warning',
    content: 'success',
    event: 'success',
};
